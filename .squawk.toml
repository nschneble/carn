pg_version = "18.0"            # must match the image in compose.yaml

# prisma opens no transaction of its own, so this is false; each migration
# carries an explicit BEGIN/COMMIT that squawk reads straight from the sql
assume_in_transaction = false
