{
  "story": "A visitor reads the repo index, then a repo, then one with no commits",
  "actions": [
    { "action": "repo-index" },
    { "action": "repo-show" },
    { "action": "sparrow-show" }
  ]
}
